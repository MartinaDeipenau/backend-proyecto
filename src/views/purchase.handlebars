<div class="container mt-5">
    <h1 class="text-center">Order Complete</h1>
    <div class="card p-4">
        <p><strong>Order Identification:</strong> {{orders.code}}</p>
        <p><strong>Date:</strong> {{orders.pucharse_datetime}}</p>
        <p><strong>Total Amount:</strong> €{{orders.amount}}</p>
        <p><strong>Purchaser:</strong> {{orders.purchaser}}</p>
    </div>
    <div class="text-center mt-4">
        <form action="/api/products/" method="get">
            <button class="btn btn-primary btn-lg px-5" type="submit">HOME</button>
        </form>
    </div>
</div>